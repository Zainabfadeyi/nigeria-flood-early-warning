{
  "name": "@nigeria-flood/actor",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/main.js",
    "test": "npm run build && node --test $(find dist -name '*.test.js')",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@nigeria-flood/shared": "*",
    "apify": "^3.2.6",
    "pdf-parse": "^1.1.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/pdf-parse": "^1.1.4",
    "typescript": "^5.6.3"
  }
}
